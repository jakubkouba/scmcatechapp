<%= form_with(model: @pupil, local: true) do |form| %>
  <% if pupil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pupil.errors.count, "error") %> prohibited this pupil from being saved:</h2>

      <ul>
      <% pupil.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pupil_name %>
  </div>

  <div class="field">
    <%= form.label :surname %>
    <%= form.text_field :surname, id: :pupil_surname %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :pupil_description %>
  </div>

  <div class="field">
    <%= form.label :points_total %>
    <%= form.number_field :points_total, id: :pupil_points_total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
